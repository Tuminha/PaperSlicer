from typing import Dict


class SectionExtractor:
    """Placeholder: naive section splitting by keywords (unused if TEI path available)."""

    def extract(self, text: str) -> Dict[str, str]:
        # Minimal: return empty mapping; TEI path provides sections.
        return {}
